/**
 * WebGL 렌더링에 사용되는 상수 모음
 */
export const WEBGL_CONSTANTS = Object.freeze({
    SHAPE_TEXTURE_SIZE: 96,
    BATCH_VERTEX_SIZE: 8,
    COLOR_CACHE_LIMIT: 256,
    DEFAULT_BACKGROUND_COLOR: Object.freeze([0.125, 0.125, 0.125, 1.0])
});
